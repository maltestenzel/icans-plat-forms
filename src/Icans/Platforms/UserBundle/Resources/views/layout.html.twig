{#
 # Stubs the original file of the FOSUserBundle, so that overlayed modules do not extend the default html skeleton.
 #
 # Author: Sascha 's10z' Schulz
 # origin: RM
 # originalFile: /vendor/friendsofsymfony/user-bundle/FOS/UserBundle/Resources/views/layout.html.twig
 #}
{% block stage %}
    {{ block('fos_user_content') }}
{% endblock stage %}