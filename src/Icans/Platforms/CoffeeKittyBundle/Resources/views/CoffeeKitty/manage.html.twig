{% extends 'IcansPlatformsCafManBundle::base.html.twig' %}
{% block stage %}
    <div class="page-header">
        <h1>{{ 'page.manageCoffeKittys.title'|trans }}</h1>
    </div>

    <div class="row">
        <div class="span6">
            {# @TODO Data for the overview is acutally merged into the mangager.
                     Clean this up for separation of concerns and module reusage. #}
            {% include 'IcansPlatformsCoffeeKittyBundle:CoffeeKitty:overview.html.twig' %}
        </div>
    </div>

    <hr />

    <div class="row">
        <div class="span6">
            <h2>{{ 'module.createKitty.title'|trans }}</h2>
            {{ create_form.content|raw }}
        </div>
    </div>

    <hr />

    {# inject the search form and result
        @TODO: improve injection mechanism.
    #}
    {{ search_form.content|raw }}

    <hr />
{% endblock stage %}