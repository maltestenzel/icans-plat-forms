{#
    Author:    Thorsten 'stepo' Hallwas
    origin:    M
    copyright: ${date} ICANS GmbH
 #}
<form action="{{ path('coffeekitty_editprice', {'kittyId' : kittyId}) }}" method="post" {{ form_enctype(form) }} class="editprice-kitty-form">
    {{ form_widget(form) }}
    <button type="submit" class="editprice-kitty-primary-action">
        {{ 'editpriceKitty.submit'|trans }}
    </button>
</form>
